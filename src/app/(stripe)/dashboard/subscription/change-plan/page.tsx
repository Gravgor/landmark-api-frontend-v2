import SubscriptionChangePlan from "@/components/dashboard/dashboard-change-plan.tsx";
import { Suspense } from "react";

export default function Page() {
    return <Suspense><SubscriptionChangePlan /></Suspense>
}